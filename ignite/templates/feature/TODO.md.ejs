---
destinationDir: features/<%= props.kebabCaseName %>
filename: TODO.md
---
# <%= props.pascalCaseName %> — feature TODO

> **This file is your working memory.** Expand it into a concrete plan BEFORE
> writing code, keep it updated as you go, and never tick a box without
> evidence. If your session is interrupted or your context is compacted, this
> file is what lets you (or the next agent) resume without re-deriving the work.
>
> Evidence means a real artefact: a test name, a command you ran, a screen and
> state you actually looked at, or the migration you checked. "Looks right" is
> not evidence.

Generated by `pnpm ignite feature <%= props.kebabCaseName %> --role=<%= props.role %>`.
Read `AGENTS.md` first. Delete guidance lines as you replace them with real content.

---

## 1. Scope

**Building:**
- [ ] _Replace with one sentence per thing this feature delivers._

**Not building (out of scope):**
- [ ] _Name the adjacent things you will deliberately NOT touch, so the PR stays small._

## 2. User stories

_Write the concrete stories before coding. Each becomes an acceptance check._

- [ ] As a <%= props.role === "preparation" ? "preparation employee" : "customer" %>, I want … so that …
- [ ] As a …, I want … so that …

## 3. Dependencies

- [ ] Features this depends on (import only via their public API `@/features/<name>`):
- [ ] Foundation modules used (`@/core/...`, `@/components/...`):
- [ ] Anything this feature BLOCKS (say so, so other agents know):

## 4. Data contract

> Verify against `supabase/migrations/*.sql`. The migrations are the truth —
> the Flutter reference is NOT. Never invent a table, column, or RPC.

- [ ] RPCs used (exact name + args + return shape):
- [ ] Tables read directly (and the RLS policy that permits it):
- [ ] Zod schema written for every RPC payload (`jsonb` arrives as the wide `Json` type)
- [ ] Confirmed this role is actually allowed the access (see `docs/data-and-supabase.md`)

## 5. UI surfaces

- [ ] Screens:
- [ ] Components:
- [ ] Routes added under `<%= props.routeDir %>/`:

## 6. States

Every data-driven surface needs these considered. Tick what applies; strike out what genuinely does not.

- [ ] Loading (skeleton preferred over a spinner when the shape is known)
- [ ] Empty — with somewhere to go next
- [ ] Error — uses `ErrorState` / `InlineError`, shows `AppError.userMessage`
- [ ] Retry
- [ ] Success
- [ ] Mutation in flight (controls disabled, duplicate taps ignored)
- [ ] Disabled
- [ ] Concurrency / stale state (the record moved while the screen was open)

## 7. Responsive behaviour

- [ ] Tablet portrait (768–1023) checked
- [ ] Tablet landscape (1024+) checked
- [ ] Narrow web preview (<768) checked
- [ ] Uses `useLayout()` / Tailwind breakpoints, not ad-hoc pixel maths
- [ ] Safe area handled via `<Screen>`

## 8. Accessibility

- [ ] Touch targets at least 48dp (`h-touch`)
- [ ] Every control has an accessible name (label, or `accessibilityLabel` for icon-only)
- [ ] Selected / disabled / busy state is announced, not just coloured
- [ ] Text scaling at 200% does not clip or overlap
- [ ] Errors announced (`accessibilityLiveRegion`)
- [ ] Nothing essential depends on hover
- [ ] Reduced-motion respected

<% if (props.layers.includes("state")) { -%>
## 9. Persistence

- [ ] What is persisted, and under which `storageKey()`
- [ ] Scoped to the active profile where it holds user data
- [ ] Write FAILURE is surfaced, not swallowed (`StorageWriteResult`)
- [ ] Restore failure degrades safely (memory-only, not a crash)

<% } -%>
<% if (props.realtime) { -%>
## 10. Realtime

- [ ] Realtime is used as an INVALIDATION SIGNAL only — never as a second source of truth
- [ ] Channel is removed on unmount
- [ ] Confirmed this role can actually receive the events (RLS applies; only `orders` is published)

<% } -%>
## 11. Security / RLS

- [ ] No RLS bypass, no new grant, no invented security-definer function
- [ ] Role guards in the UI treated as UX only — the database is the boundary
- [ ] No secret, token, or key logged or rendered
- [ ] Untrusted RPC payloads validated at the boundary

## 12. Edge cases

- [ ] _List the ones specific to this feature._

## 13. Tests

> Write the failing test first where behaviour is testable, confirm it fails for
> the RIGHT reason, then implement. Do not weaken a test to make it pass.

- [ ] Unit: schema parsing / domain logic
- [ ] Unit: error mapping (which `AppError.kind` each failure produces)
- [ ] Component: renders loading, empty, error, and populated states
- [ ] Component: the main interaction works and cannot be double-fired
- [ ] Accessibility assertions (`getByRole`, `toBeDisabled`)

## 14. Verification

Record the evidence here as you go.

- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm format:check`
- [ ] `pnpm test` — name the test files that cover this feature:
- [ ] `pnpm web` and opened the screen in a browser at a narrow and a wide width
- [ ] Android checked, or explicitly noted as unverified

## 15. Definition of Done

- [ ] Every box above is ticked or explicitly struck out with a reason
- [ ] No `TODO` left in shipped code without an owner
- [ ] Shared/core files touched: _list them — aim for zero_
- [ ] `pnpm verify` passes
